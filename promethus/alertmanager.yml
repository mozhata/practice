global:
  # The smarthost and SMTP sender used for mail notifications.
  smtp_smarthost: 'smtp.163.com:25'
  smtp_from: "zyk7676@163.com"
  smtp_auth_username: "zyk7676@163.com"
  smtp_auth_password: 'dx853556721'
  # The auth token for Hipchat.
  hipchat_auth_token: 'dx853556721'
  # Alternative host for Hipchat.
  # hipchat_url: 'https://hipchat.foobar.org/'

route:  
  group_by: [Alertname]
  # Send all notifications to me.
  receiver: email-me
  # When a new group of alerts is created by an incoming alert, wait at
  # least 'group_wait' to send the initial notification.
  # This way ensures that you get multiple alerts for the same group that start
  # firing shortly after another are batched together on the first
  # notification.
  group_wait: 30s

  # When the first notification was sent, wait 'group_interval' to send a batch
  # of new alerts that started firing for that group.
  group_interval: 5m

  # If an alert has successfully been sent, wait 'repeat_interval' to
  # resend them.
  repeat_interval: 3h

# templates:  
# - '/etc/ALERTMANAGER_PATH/default.tmpl'

receivers:  
- name: email-me
  email_configs:
  - to: "mozhata@aliyun.com"
    # smtp_smarthost: 'localhost:25'
    # from: 'alertmanager@example.org'
    # # smarthost: smtp.gmail.com:587
    # smarthost:  smtp.163.com
    # html: '{{ template "email.default.html" . }}'
    # auth_username: "zyk7676@163.com"
    # auth_password: "dx853556721"
    # auth_secret: "dx853556721"
    # auth_identity: "dx853556721"
    # require_tls: "dx853556721"
